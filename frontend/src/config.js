// This file will be auto-generated during deployment
// For local development, you can manually set these values

const config = {
  AUTH_API_URL: window.CONFIG?.AUTH_API_URL || 'http://localhost:3001',
  WEBSOCKET_URL: window.CONFIG?.WEBSOCKET_URL || 'ws://localhost:3001'
};

export default config;
